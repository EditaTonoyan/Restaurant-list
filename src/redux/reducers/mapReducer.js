const initialState = {
  center: {
    lat: 40.183333,
    lng: 44.516667,
  },
  zoom: 11,
};
const mapReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default mapReducer;
